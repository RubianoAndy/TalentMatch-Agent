# resultados/ (SALIDA)

Aquí el agente guarda lo que encuentra:

- `<perfil>_<fecha>_vacantes.json` — vacantes en JSON (URL, empresa, correo, salario…).
- `<perfil>_<fecha>_vacantes.csv` — las mismas vacantes en CSV (abrible en Excel).
- `<perfil>_<fecha>_reporte.md` — reporte legible con tabla resumen y fichas.
- `<perfil>_cv_texto.txt` — (opcional) texto crudo extraído de la HV.

No edites estos archivos a mano si quieres que el agente los reutilice.
